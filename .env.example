# Windows launcher:
# npm run dev:windows
# If .env is missing, scripts/start-dev.ps1 falls back to the local Ollama preset for that run.
# Preferred cross-platform dev path:
# docker compose up --build
# If the app runs in Docker and your AI service runs on the host machine, use host.docker.internal instead of localhost.

AI_PROVIDER=openai-compatible
AI_API_KEY=your_key_here
AI_BASE_URL=
AI_CHAT_MODEL=gpt-4o-mini
AI_EMBEDDING_MODEL=text-embedding-3-small

# LiteLLM proxy mode:
# AI_PROVIDER=litellm
# LITELLM_PROXY_URL=http://127.0.0.1:4000
# LITELLM_API_KEY=anything
# LITELLM_CHAT_MODEL=game-chat
# LITELLM_EMBEDDING_MODEL=game-embedding
# LITELLM_MASTER_KEY=sk-1234

# Ollama local mode (Windows smoke-test path):
# AI_PROVIDER=ollama
# OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
# OLLAMA_API_KEY=ollama
# OLLAMA_CHAT_MODEL=gemma3:4b
# OLLAMA_EMBEDDING_MODEL=embeddinggemma

# Backward-compatible legacy names still work:
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_MODEL=
# OPENAI_EMBEDDING_MODEL=
PORT=3000
